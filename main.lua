local TweenService = game:GetService "TweenService" local RunService = game:GetService "RunService" local plr = game:GetService "Players".LocalPlayer local playerGui = plr:WaitForChild "PlayerGui" local blastGui = playerGui:WaitForChild "BLAST_MainGui" local mainFrame = blastGui:FindFirstChild "MainFrame" local sideFrame = blastGui:FindFirstChild "SideFrame" if mainFrame then mainFrame.Visible = false end if sideFrame then sideFrame.Visible = false end local gui = Instance.new "ScreenGui" gui.Name = "Intro" gui.ResetOnSpawn = false gui.Parent = playerGui local Main = Instance.new "Frame" Main.Name = "Main" Main.Active = true Main.BackgroundColor3 = Color3.new(0.20392157137394, 0.20392157137394, 0.20392157137394) Main.BorderSizePixel = 0 Main.Position = UDim2.new(0.5, -175, 0.5, -100) Main.Size = UDim2.new(0, 350, 0, 200) Main.Parent = gui local Holder = Instance.new "Frame" Holder.Name = "Holder" Holder.BackgroundColor3 = Color3.new(0.17647059261799, 0.17647059261799, 0.17647059261799) Holder.BorderSizePixel = 0 Holder.ClipsDescendants = true Holder.Size = UDim2.new(1, 0, 1, 0) Holder.Parent = Main local holderGradient = Instance.new "UIGradient" holderGradient.Parent = Holder holderGradient.Rotation = 30 holderGradient.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 1), NumberSequenceKeypoint.new(1, 1), }) local Title = Instance.new "TextLabel" Title.Name = "Title" Title.BackgroundColor3 = Color3.new(1, 1, 1) Title.BackgroundTransparency = 1 Title.Font = Enum.Font.Gotham Title.Parent = Holder Title.Position = UDim2.new(0, -190, 0, 15) Title.Size = UDim2.new(0, 100, 0, 50) Title.Text = "NEE" Title.TextColor3 = Color3.new(1, 1, 1) Title.TextSize = 50 Title.TextTransparency = 1 local Desc = Instance.new "TextLabel" Desc.Name = "Desc" Desc.BackgroundColor3 = Color3.new(1, 1, 1) Desc.BackgroundTransparency = 1 Desc.Font = Enum.Font.SourceSans Desc.Parent = Holder Desc.Position = UDim2.new(0, -230, 0, 60) Desc.Size = UDim2.new(0, 180, 0, 25) Desc.Text = "Ultimate Executor Suite" Desc.TextColor3 = Color3.new(1, 1, 1) Desc.TextSize = 18 Desc.TextTransparency = 1 local StatusText = Instance.new"TextLabel" StatusText.Name = "StatusText" StatusText.BackgroundColor3 = Color3.new(1, 1, 1) StatusText.BackgroundTransparency = 1 StatusText.Font = Enum.Font.SourceSans StatusText.Parent = Holder StatusText.Position = UDim2.new(0, 20, 0, 110) StatusText.Size = UDim2.new(0, 250, 0, 25) StatusText.Text = "Initializing" StatusText.TextColor3 = Color3.new(1, 1, 1) StatusText.TextSize = 14 StatusText.TextTransparency = 1 local ProgressBar = Instance.new "Frame" ProgressBar.Name = "ProgressBar" ProgressBar.BackgroundColor3 = Color3.new(0.20392157137394, 0.20392157137394, 0.20392157137394) ProgressBar.BorderSizePixel = 0 ProgressBar.Parent = Holder ProgressBar.Position = UDim2.new(0, 110, 0, 145) ProgressBar.Size = UDim2.new(0, 0, 0, 4) local Bar = Instance.new "Frame" Bar.Name = "Bar" Bar.BackgroundColor3 = Color3.new(0.2392156869173, 0.56078433990479, 0.86274510622025) Bar.BorderSizePixel = 0 Bar.Parent = ProgressBar Bar.Size = UDim2.new(0, 0, 1, 0) local barImage = Instance.new "ImageLabel" barImage.Name = "ImageLabel" barImage.BackgroundColor3 = Color3.new(1, 1, 1) barImage.BackgroundTransparency = 1 barImage.Image = "rbxassetid://2764171053"barImage.ImageColor3 = Color3.new(0.17647059261799, 0.17647059261799, 0.17647059261799) barImage.Parent = ProgressBar barImage.ScaleType = Enum.ScaleType.Slice barImage.Size = UDim2.new(1, 0, 1, 0) barImage.SliceCenter = Rect.new(2, 2, 254, 254) local Creator = Instance.new "TextLabel" Creator.Name = "Creator" Creator.BackgroundColor3 = Color3.new(1, 1, 1) Creator.BackgroundTransparency = 1 Creator.Font = Enum.Font.SourceSans Creator.Parent = Main Creator.Position = UDim2.new(1, -110, 1, -20) Creator.Size = UDim2.new(0, 105, 0, 20) Creator.Text = "Made by CloneCrafts and Engine by F3X" Creator.TextColor3 = Color3.new(1, 1, 1) Creator.TextSize = 14 Creator.TextXAlignment = Enum.TextXAlignment.Right local creatorGradient = Instance.new "UIGradient" creatorGradient.Parent = Creator creatorGradient.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 1), NumberSequenceKeypoint.new(1, 1), }) local Version = Instance.new "TextLabel" Version.Name = "Version" Version.BackgroundColor3 = Color3.new(1, 1, 1) Version.BackgroundTransparency = 1 Version.Font = Enum.Font.SourceSans Version.Parent = Main Version.Position = UDim2.new(1, -110, 1, -35) Version.Size = UDim2.new(0, 105, 0, 20) Version.Text = "Beta v1.0" Version.TextColor3 = Color3.new(1, 1, 1) Version.TextSize = 14 Version.TextXAlignment = Enum.TextXAlignment.Right local versionGradient = Instance.new "UIGradient" versionGradient.Parent = Version versionGradient.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 1), NumberSequenceKeypoint.new(1, 1), }) local Outlines = Instance.new "ImageLabel" Outlines.Name = "Outlines" Outlines.BackgroundColor3 = Color3.new(1, 1, 1) Outlines.BackgroundTransparency = 1 Outlines.BorderSizePixel = 0 Outlines.Image = "rbxassetid://1427967925" Outlines.Parent = Main Outlines.Position = UDim2.new(0, -5, 0, -5) Outlines.ScaleType = Enum.ScaleType.Slice Outlines.Size = UDim2.new(1, 10, 1, 10) Outlines.SliceCenter = Rect.new(6, 6, 25, 25) local outlinesGradient = Instance.new "UIGradient" outlinesGradient.Parent = Outlines outlinesGradient.Rotation = -30 outlinesGradient.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 1), NumberSequenceKeypoint.new(1, 1), }) local backGradient = Instance.new "UIGradient" backGradient.Parent = Main backGradient.Rotation = -30 backGradient.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 1), NumberSequenceKeypoint.new(1, 1), }) local renderStepped = RunService.RenderStepped local signalWait = renderStepped.Wait local function fastwait(s) if not s then return signalWait(renderStepped) end local start = tick() while tick() - start < s do signalWait(renderStepped) end end local function tweenNumber(n, ti, func) local tweenVal = Instance.new "IntValue" tweenVal.Value = 0 tweenVal.Changed:Connect(func) local tween = TweenService:Create(tweenVal, ti, {Value = n}) tween:Play() tween.Completed:Connect(function() tweenVal:Destroy() end) end StatusText.Text = "Initializing Library" local ti = TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out) tweenNumber(100, ti, function(val) val = val / 200 local start = NumberSequenceKeypoint.new(0, 0) local a1 = NumberSequenceKeypoint.new(val, 0) local a2 = NumberSequenceKeypoint.new(math.min(0.5, val + math.min(0.05, val)), 1) if a1.Time == a2.Time then a2 = a1 end local b1 = NumberSequenceKeypoint.new(1 - val, 0) local b2 = NumberSequenceKeypoint.new(math.max(0.5, 1 - val - math.min(0.05, val)), 1) if b1.Time == b2.Time then b2 = b1 end local goal = NumberSequenceKeypoint.new(1, 0) backGradient.Transparency = NumberSequence.new({start, a1, a2, b2, b1, goal}) outlinesGradient.Transparency = NumberSequence.new({start, a1, a2, b2, b1, goal}) end) fastwait(0.4) tweenNumber(100, ti, function(val) val = val / 166.66 local start = NumberSequenceKeypoint.new(0, 0) local a1 = NumberSequenceKeypoint.new(val, 0) local a2 = NumberSequenceKeypoint.new(val + 0.01, 1) local goal = NumberSequenceKeypoint.new(1, 1) holderGradient.Transparency = NumberSequence.new({start, a1, a2, goal}) end) TweenService:Create(Title, ti, {Position = UDim2.new(0, 60, 0, 15), TextTransparency = 0}):Play() TweenService:Create(Desc, ti, {Position = UDim2.new(0, 20, 0, 60), TextTransparency = 0}):Play() local function rightTextTransparency(obj) tweenNumber(100, ti, function(val) val = val / 100 local a1 = NumberSequenceKeypoint.new(1 - val, 0) local a2 = NumberSequenceKeypoint.new(math.max(0, 1 - val - 0.01), 1) if a1.Time == a2.Time then a2 = a1 end local start = NumberSequenceKeypoint.new(0, a1 == a2 and 0 or 1) local goal = NumberSequenceKeypoint.new(1, 0) obj.Transparency = NumberSequence.new({start, a2, a1, goal}) end) end rightTextTransparency(versionGradient) rightTextTransparency(creatorGradient) fastwait(0.9) local progressTI = TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out) TweenService:Create(StatusText, progressTI, {Position = UDim2.new(0, 20, 0, 120), TextTransparency = 0}):Play() TweenService:Create(ProgressBar, progressTI, {Position = UDim2.new(0, 60, 0, 145), Size = UDim2.new(0, 100, 0, 4)}):Play() fastwait(0.25) local function setProgress(text, n) StatusText.Text = text TweenService:Create(Bar, progressTI, {Size = UDim2.new(n, 0, 1, 0)}):Play() end local function waitForReady(displayText, progressVal, checkFunc, timeout) setProgress(displayText, progressVal) local startTime = tick() local minDisplay = 0.4 while true do local elapsed = tick() local conditionMet = checkFunc() if conditionMet and elapsed >= minDisplay then break end if elapsed > (timeout or 15) then break 	end fastwait(0.05) end end local keyFrames = { "MainFrame", "SideFrame", "ProfileFrame", "ChatFrame", "MusicPlayerFrame", "LoudMusicPlayerFrame", "KickFrame", "ErrorFrame", "OutputFrame", "ServerAnnouncementFrame", "ChatTagFrame", "PrivateServerMakerFrame", "NormalPistolWarningFrame", } waitForReady("Initializing Library", 0.10, function() for _, frameName in ipairs(keyFrames) do if not blastGui:FindFirstChild(frameName) then return false end end return true end, 15) local keyRemotes = {"SayMessage", "KickPlayer", "RunErrorMessage", "PlayMusic"} waitForReady("Connecting to Server", 0.25, function() for _, remoteName in ipairs(keyRemotes) do local remote = blastGui:FindFirstChild(remoteName) if not remote or not remote:IsA "RemoteEvent" then return false end end local chatTagFrame = blastGui:FindFirstChild "ChatTagFrame" if chatTagFrame then if not chatTagFrame:FindFirstChild "ChangeColor" or not chatTagFrame:FindFirstChild "ButtonPressedEvent" then return false end end return true end, 15) waitForReady("Loading Scripts", 0.40, function() local mf = blastGui:FindFirstChild "MainFrame" if not mf then return false end local executorBtn = mf:FindFirstChild "ExecutorButton", true local settingsBtn = mf:FindFirstChild "SettingsButton", true local scriptHubBtn = mf:FindFirstChild "ScriptHubButton", true local blindFrame = mf:FindFirstChild "BlindFrame" local movingVal = blindFrame and blindFrame:FindFirstChild "Moving" local executor = mf:FindFirstChild "Executor" local settings = mf:FindFirstChild "Settings" local scriptHub = mf:FindFirstChild "ScriptHub" return executorBtn and settingsBtn and scriptHubBtn and blindFrame and movingVal and executor and settings and scriptHub end, 10) waitForReady("Setting Up UI", 0.55, function() local sf = blastGui:FindFirstChild "SideFrame"	if not sf then return false end local logo = sf:FindFirstChild "Logo" local user = sf:FindFirstChild "User" local chatBtn = sf:FindFirstChild "ChatBtnBg" local musicBtn = sf:FindFirstChild "MusicBtnBg" local kickBtn = sf:FindFirstChild "KickBtnBg" local version = sf:FindFirstChild "Version" local sideScript = sf:FindFirstChild "SideFrameScript" local profileScript = sf:FindFirstChild "ProfileScript" return logo and user and chatBtn and musicBtn and kickBtn and version and sideScript and profileScript end, 10) local assetProgressStart = 0.55 local assetProgressEnd = 0.85 setProgress("Loading Assets", assetProgressStart) local assetInstances = {} for _, desc in ipairs(blastGui:GetDescendants()) do if desc:IsA("ImageLabel") or desc:IsA "ImageButton" then if desc.Image ~= "" and desc.Image ~= "rbxasset://" then table.insert(assetInstances, desc) end elseif desc:IsA "Decal" or desc:IsA "Texture" then if desc.Texture ~= "" then table.insert(assetInstances, desc) end elseif desc:IsA "Sound" then if desc.SoundId ~= "" and desc.SoundId ~= "rbxasset://" then table.insert(assetInstances, desc) end elseif desc:IsA "VideoFrame" then if desc.Video ~= "" then table.insert(assetInstances, desc) end end end local totalAssets = #assetInstances local assetStartTime = tick() local assetMinDisplay = 0.5 local assetTimeout = 45 if totalAssets > 0 then while true do local elapsed = tick() - assetStartTime local loadedCount = 0 for _, asset in ipairs(assetInstances) do if asset:IsA "Sound" then if asset.IsLoaded or asset.TimeLength > 0 then loadedCount = loadedCount + 1 end elseif asset:IsA "ImageLabel" or asset:IsA "ImageButton" then if asset.IsLoaded then loadedCount = loadedCount + 1 end elseif asset:IsA "Decal" or asset:IsA "Texture" then loadedCount = loadedCount + 1 elseif asset:IsA "VideoFrame" then if asset.IsLoaded then loadedCount = loadedCount + 1 end else loadedCount = loadedCount + 1 end end local ratio = loadedCount / totalAssets local dynamicProgress = assetProgressStart + (assetProgressEnd - assetProgressStart) * ratio setProgress("Loading Assets (" .. loadedCount .. "/" .. totalAssets .. ")", dynamicProgress) if loadedCount >= totalAssets and elapsed >= assetMinDisplay then break end if elapsed > assetTimeout then break end fastwait(0.05) end else fastwait(assetMinDisplay) end waitForReady("Loading Player Data", 0.90, function() local char = plr.Character if not char then return false end local humanoid = char:FindFirstChildOfClass("Humanoid") local hrp = char:FindFirstChild("HumanoidRootPart") return humanoid ~= nil and hrp ~= nil end, 30) setProgress("Complete", 1) fastwait(0.5) TweenService:Create(Title, progressTI, {TextTransparency = 1}):Play() TweenService:Create(Desc, progressTI, {TextTransparency = 1}):Play() TweenService:Create(Version, progressTI, {TextTransparency = 1}):Play() TweenService:Create(Creator, progressTI, {TextTransparency = 1}):Play() TweenService:Create(StatusText, progressTI, {TextTransparency = 1}):Play() TweenService:Create(ProgressBar, progressTI, {BackgroundTransparency = 1}):Play() TweenService:Create(Bar, progressTI, {BackgroundTransparency = 1}):Play() TweenService:Create(barImage, progressTI, {ImageTransparency = 1}):Play() tweenNumber(100, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), function(val) val = val / 250 local start = NumberSequenceKeypoint.new(0, 0) local a1 = NumberSequenceKeypoint.new(0.6 + val, 0) local a2 = NumberSequenceKeypoint.new(math.min(1, 0.601 + val), 1) if a1.Time == a2.Time then a2 = a1 end local goal = NumberSequenceKeypoint.new(1, a1 == a2 and 0 or 1) holderGradient.Transparency = NumberSequence.new({start, a1, a2, goal}) end) fastwait(0.5) Main.BackgroundTransparency = 1 outlinesGradient.Rotation = 30 tweenNumber(100, ti, function(val) val = val / 100 local start = NumberSequenceKeypoint.new(0, 1) local a1 = NumberSequenceKeypoint.new(val, 1) local a2 = NumberSequenceKeypoint.new(math.min(1, val + math.min(0.05, val)), 0) if a1.Time == a2.Time then a2 = a1 end local goal = NumberSequenceKeypoint.new(1, a1 == a2 and 1 or 0) outlinesGradient.Transparency = NumberSequence.new({start, a1, a2, goal}) holderGradient.Transparency = NumberSequence.new({start, a1, a2, goal}) end) fastwait(0.45) if mainFrame then mainFrame.Visible = true mainFrame.Position = UDim2.new(0.5, 0, -0.6, 0) local blindFrame = mainFrame:FindFirstChild "BlindFrame" local movingVal = blindFrame and blindFrame:FindFirstChild "Moving" if blindFrame and movingVal then movingVal.Value = true blindFrame.Size = UDim2.new(1, 0, 1, 0) mainFrame:TweenPosition(UDim2.new(0.5, 0, 0.5, 0), Enum.EasingDirection.Out, Enum.EasingStyle.Back, 2) fastwait(2) blindFrame:TweenSize(UDim2.new(0, 0, 1, 0), Enum.EasingDirection.InOut, Enum.EasingStyle.Quad, 1) fastwait(1) movingVal.Value = false else mainFrame:TweenPosition(UDim2.new(0.5, 0, 0.5, 0), Enum.EasingDirection.Out, Enum.EasingStyle.Back, 2) fastwait(2) end end if sideFrame then sideFrame.Visible = true sideFrame.Position = UDim2.new(-0.1, 0, 0.11, 0) sideFrame:TweenPosition(UDim2.new(-0.000968, 0, 0.11, 0), Enum.EasingDirection.Out, Enum.EasingStyle.Back, 1) end fastwait(1) gui:Destroy()
